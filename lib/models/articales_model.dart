class ArticalesModel {
  final String title, subTitle;
  final int image;
  bool isSaved;

  ArticalesModel({
    required this.title,
    required this.subTitle,
    required this.image,
    required this.isSaved,
  });
}